# Copy เป็น .env แล้วแก้ค่าก่อน deploy
# cp .env.example .env

# ── Database ──────────────────────────────
DB_NAME=ebpf_research
DB_USER=ebpf
DB_PASSWORD=change_me_strong_password_here

# ── Domain (ใช้ใน nginx ถ้าต้องการ) ──────
DOMAIN=localhost
